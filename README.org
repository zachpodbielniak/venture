#+title: VENTURE
#+description: An ERP/CRM hybrid in C for running a portfolio of business ventures
#+authors: Zach Podbielniak
#+categories: projects
#+created: [2026-08-14]
#+updated: [2026-09-11]
#+version: 0.6.0

* VENTURE

VENTURE is an ERP and CRM in one, written in C on GLib/GObject, for running a portfolio of small business ventures rather than one company. It tracks ventures and their performance, inventory, contacts and deals, expenses, tax write-offs and P&L across multiple entities — businesses and personal alike — with AI as a first-class participant.

It is built for one operator running several things at once: a book imprint, an Etsy shop, a newsletter, whatever comes next. The design assumption throughout is that adding a new kind of venture should cost a field table, not a project.

** Two binaries

| Binary       | What it is                                                      |
|--------------+-----------------------------------------------------------------|
| =venture=    | The server. REST API, HTMX web UI, reporting, AI                 |
| =venturectl= | The CLI, and =venturectl mcp=, a stdio MCP server over that API   |

Both link every vendored dependency statically, so neither has a runtime dependency on =libyaml-glib.so=, =libhtmx-glib.so=, =libai-glib.so=, =liborm-glib.so=, =libpodomation.so= or =libcrispy.so=. Only true system libraries stay dynamic.

Either can be built alone. =make venturectl= works on a machine that cannot build the server at all — no libpq, no sqlite headers, no readline — because the CLI links only =libventure-core.a= and yaml-glib.

** Building

#+begin_src sh
git submodule update --init --recursive
make DEBUG=1              # debug build into build/debug
make                      # release build into build/release
make test                 # GTest suite
make run                  # run the server against a scratch SQLite database
#+end_src

See =make help= for the full target list and =make show-config= for the resolved configuration.

=make DEBUG=1 gir= builds the Orm, AiGlib and HtmxGlib introspection dependencies from their pinned submodules before scanning Venture. The optional shared library uses those same dependency shared libraries so imported types have one runtime identity; the server, CLI and tests retain static dependency linking. =make DEBUG=1 install-gir DESTDIR=/path/to/stage= stages Venture and dependency GIR/typelib files together with the required shared libraries and SONAME links. =BUILD_GIR=1= also includes this installation in the ordinary =install= target.

*** Fedora build dependencies

=make list-deps= prints the package names to layer into an image build:

#+begin_src
gcc make pkgconf-pkg-config glib2-devel libyaml-devel json-glib-devel
libsoup3-devel libxml2-devel sqlite-devel libpq-devel readline-devel
gobject-introspection-devel
#+end_src

Plus =libetpan-devel= and =libgudev-devel=, which podomation's mail and
device modules need. They are easy to miss: podomation resolves its whole
dependency set in one =pkg-config= call, so a single missing package empties
the flags and the build fails on a missing =glib.h=.

*** Containers

#+begin_src sh
podman-compose up --build                        # local, SQLite, loopback
podman-compose -f compose.deploy.yaml up -d      # example: PostgreSQL
#+end_src

The image is =quay.io/zachpodbielniak/venture=, built from
[[file:Containerfile][Containerfile]] on =registry.fedoraproject.org/fedora:44=. See
[[file:docs/containers.org][the container documentation]] for what the two compose files differ on and why.

** Dependencies

All are git submodules under =deps/= and are treated as the canonical copies. Several of them vendor each other. The build deliberately does *not* redirect their sub-dependency paths: =ar t= confirms no dependency's archive embeds another's objects, so each carries unresolved references that this tree's single canonical copy satisfies at link time.

| Dependency  | Role                                                        |
|-------------+-------------------------------------------------------------|
| =yaml-glib= | YAML parsing and generation for GObject                     |
| =htmx-glib= | HTMX-powered web application framework                      |
| =ai-glib=   | AI providers with tool-call support                         |
| =orm-glib=  | ORM and serialization; SQLite and PostgreSQL dialects       |
| =podomation=| Event-driven automation engine, embedded in the server       |
| =crispy=    | Runtime C scripting, used for compiled config and plugins   |
| =mail-glib= | Transactional MIME and SMTP delivery, with nested otel-glib |

** Design

*** Everything derives from GObject properties

=VentureEntity= is the abstract base every persisted record derives from. Define a subclass, install GObject properties, register it, and you automatically get a database table, REST CRUD, JSON and YAML serialisation, a web form, AI tools, audit records and CLI subcommands — without writing SQL, a route, a serialiser or a tool definition.

Per-property persistence hints (=primary_key=, =indexed=, =unique=, =immutable=, =sensitive=, =searchable=, =transient=) are attached with =venture_entity_class_set_column_flags()=. Foreign keys are declared with =venture_entity_class_set_reference()=. That metadata is what the schema builder, the form renderer, the AI schema generator and the audit diff all read.

*** Money is never a double

=VentureMoney= stores an exact integer count of minor units alongside the ISO 4217 code and that currency's exponent. Every operation checks for overflow; rounding, where unavoidable, is half to even; =venture_money_allocate()= splits an amount so the parts sum back to the original exactly. Arithmetic across currencies is refused rather than guessed.

*** Extending it

Adding a record type in C is a field table and a registration, and it becomes
a table, a REST resource, a web view, AI tools and CLI subcommands at once.
See [[file:docs/data-model.org][the data model]].

There are four ways in, in increasing order of power: a YAML venture type
(no code), a podomation rule (a DSL), a crispy =.c= script compiled on
demand, and a native =.so= plugin. All four are loaded by
=VenturePluginManager= from the same configured directories. See
[[file:docs/extending.org][extending]].

*** Everything is a module, and every module is a switch

The CRM, invoicing, the knowledge bases, the git forges, the software
factory: each is a named group of record types, reports, pages and services
that the configuration turns off as one thing --

#+begin_src yaml
modules:
  crm: false
#+end_src

-- and a module whose requirement is off is refused at startup, naming both,
rather than run half-working. A disabled module's types disappear from REST,
the schema, the forms, the AI and the CLI at once, because all of them ask
one registry. Nothing is deleted; turn it back on and the rows are there.
A plugin declares a module the same way. See [[file:docs/modules.org][modules]].

*** Anything links to anything

A =record_link= joins any two records by type and id with a kind that has an
inverse -- blocks and blocked by, produces and produced by -- stored once and
read from either end. Every detail page has a Links panel; the API, the CLI
and the assistant have the same. The checks the schema cannot express run at
the save, so an approved staged link is held to the same rules as one made
by hand.

*** Pages you build

The =dashboards= module: as many pages as you like, each a grid of widgets
-- a count, a list, a breakdown, a report's figure or its table or its rows
as bars, what is due, one record, its links, the audit trail, what awaits
approval, a note, a row of buttons, a search box, and for the factory each
environment's running release and a milestone's progress. Any widget reads
any module's records or reports; the same widget answers identically on the
page, over the API and to the assistant. One dashboard can be home, a
personal one is yours alone, and a dashboard exports as JSON that imports
anywhere -- four ship as templates, the factory among them. See
[[file:docs/dashboards.org][dashboards]].

=make demo= builds and runs a throwaway instance on 127.0.0.1:8749 seeded with a small complete business — two ventures, a year of trade posted through to a general ledger, receivables and payables with their credits and refunds, a matched bank statement, fixed assets and deferrals, a sales pipeline with leads and quotes and activities, subscriptions and follow-up sequences, a support desk with service levels and a sprint, a release and an incident, and two dashboards. It prints the password when it is ready, and rebuilds its database on every run. See [[file:docs/quickstart.org][the quickstart]].

The workdesk: every record has a Watch button and a timeline; an inbox fed by the audit trail tells you about mentions, assignments, watched changes, service levels, budgets and runs; saved views, service-level policies with live clocks, macros, worklogs, sprints and bulk edits on tickets; mission control for the coding runs with a cost per success, agent budgets that refuse a run once the money is gone, and a delivery report with DORA's four keys beside the agents' cost. Ctrl+K opens a command palette. See [[file:docs/workdesk.org][the workdesk]].

*** A software factory

The =factory= module is the loop a software venture runs, as records: a
ticket is planned into a milestone, built by the forge's CI (every workflow
run arrives through the webhook as a build), assembled into a release whose
changelog is drafted from the tickets and published to the forge, deployed
to an environment, and -- when something breaks there -- raised as an
incident that points back at the release and deployment and forward at the
ticket that fixes it. A lead-time report measures the loop end to end. See
[[file:docs/factory.org][the software factory]].

*** Multiple entities, nested

An entity is a business or a personal sphere you keep books for. Every record
belongs to exactly one, and an entity may name a parent, so "my side
business" can hold the LLC trading under it. The picker in the sidebar scopes
the whole UI; selecting a parent rolls up everything beneath it. The
hierarchy decides what is shown together, never what a record is filed
against.

*** Every record type has a form

Lists, forms, controls, field order and validation all come from the type's
field table — the same metadata behind the schema, the REST API and the AI
tools. A record type gains a working form the moment it is registered,
including one from a plugin.

What the table declares is carried through rather than recomputed. A kind
derived from the C type cannot tell a long text field from a short string
one, and GObject returns properties in its own order rather than the declared
one; losing either turns a Notes box into a one-line input or a list's first
columns into empty ones.

*** A CRM that is actually relational

Companies are the outside businesses you deal with; entities are the ones you
are. Contacts belong to companies by reference, and deals, interactions and
tickets carry both. Every record has a detail page listing everything that
points at it, derived from the reference metadata rather than written per
type — so a plugin's record type appears there for free.

*** One ticket system, internal and external

A project task and a support request are the same shape, so they are the same
record with a =kind=. Comments carry an internal flag, so a reply to the
customer and a note to yourself share one thread and stay tellable apart.
=/tickets= is a kanban board with drag and drop, and a per-card form that
does the same thing without scripting.

*** AI has authority, but bounded

AI tool calls read freely. Anything that would change a record is staged as a =VentureConfirmation= carrying a diff, and applies only after approval. So is any REST write sent with =?stage=1=, which is how an outside agent holding an API token proposes a change: one queue, answered from one page, whoever asked. Every applied mutation is audited with the prompt that caused it. The policy is configurable per install and per tool; the default is =confirm_writes=, because a hallucinated tool call that rewrites an expense is a tax problem rather than a UI annoyance.

*** An outside agent gets the whole data model, generated

=venturectl mcp= is a Model Context Protocol server on stdin and stdout, so
an AI coding agent reaches VENTURE the way it reaches any other tool server —
an =.mcp.json= entry naming the command. It offers nine tools, and the record
types they accept are enumerated from =GET /api/v1/schema= at startup rather
than written down in C. A record type registered by a plugin is therefore
offered the moment it registers, which is the same promise the REST API
already makes.

The token comes from =VENTURE_TOKEN= and there is no flag for it: an MCP
server is spawned from a config file, and an argv is world-readable through
=/proc=. Writes are staged rather than applied by default, matching the
in-process AI's =confirm_writes= policy: the change is queued on the server as
a confirmation and a person approves it from the same panel the assistant's
staged writes appear in. See [[file:docs/mcp.org][the MCP documentation]].

*** Federation and offline collaboration

Opt-in federation shares explicitly selected records and fields with pinned servers, or selected read-only fields globally. Ed25519 authenticates both requests and responses, including through a TLS tunnel. The federation origin can differ from the normal web URL. Local working copies retain offline edits and reconcile against their home server with per-field three-way merges and explicit conflicts. See [[file:docs/federation.org][federation and offline sync]].

*** Conversations are records

The assistant lives in a floating launcher that opens a right-hand panel (=Ctrl+/=). Each conversation is a =chat_thread= with =chat_message= rows — stored through the same repository as everything else, so a thread resumes across page loads, restarts and machines, and the model is shown the stored transcript when a thread continues. Threads are scoped to the account that had them: the chat routes 404 on anybody else's thread, and the generic surfaces for the chat types are owner-only.

** Layout

#+begin_src
src/
  venture.h            umbrella header; the only one to include
  boxed/               VentureMoney, VentureDateRange, VentureMetric,
                       VentureFieldSpec
  interfaces/          VentureSerializable, VentureReportable,
                       VentureToolProvider
  model/               VentureEntity and every record type
  config/              YAML + compiled C configuration
  db/                  orm-glib layer, repository, query, migrations
  report/              reporting engine
  ai/                  provider wiring, tools, sessions, confirmations
  automation/          embedded podomation engine
  plugin/              plugin host
  web/                 REST API and HTMX UI
  util/                JSON, string and time helpers
  cli/                 venturectl
modules/               out-of-process podomation modules; the `venture`
                       module itself is in src/automation, because it
                       needs the context
plugins/example/       a worked native plugin, built by `make plugins`
plugins/scripts/       worked crispy plugins, never built -- the server
                       compiles them on demand
data/venture-types/    declarative venture type definitions
docs/                  org-mode documentation
tests/                 GTest suite
Containerfile          two-stage image build on fedora:44
compose.yaml           local: builds from the tree, SQLite, loopback
compose.deploy.yaml    example deployment: PostgreSQL, named volumes
#+end_src

** Status

Working and covered by tests:

- the build system, both binaries, static linking of all seven dependencies
- =VentureMoney= and the boxed types
- =VentureEntity=, the record types (31 built in), the registry
- configuration, including the compiled C configuration via crispy
- the database layer over orm-glib, the query builder, schema derivation,
  and reference integrity enforced at the save
- the reporting engine and ten built-in reports, none of which will total
  across currencies or leave an exclusion unmentioned
- authentication with per-address login rate limiting, sessions, API
  tokens, the REST API, the HTMX UI
- the AI service, its tools and the write-confirmation policy
- =venturectl=, including =venturectl mcp= — a stdio MCP server whose tool
  surface is generated from the schema, tested entirely against fixtures
- =VenturePluginManager= and all four extension mechanisms: native =.so=
  plugins, crispy =.c= scripts, podomation rules and YAML venture types
- =VentureAutomation=: the embedded podomation engine, VENTURE's =venture=
  pod module and its record-change events
- a relational CRM: companies, contacts, deals, interactions, and a detail
  page per record listing everything that references it
- ticketing, internal and external, with a kanban board, a list view and an
  inline comment composer on the ticket page
- multiple entities, nested, with per-entity scoping and roll-up
- generated create and edit forms for every record type
- list views with pagination, sortable columns and CSV export of the
  current filter
- global search across every registered record type (=Ctrl+K=)
- persistent, per-user AI conversations in a floating panel, resumable
  across sessions
- chat file attachments: an uploaded PDF or text file becomes a document
  record with its text extracted, readable by the AI — attach an invoice,
  have the expense staged for approval
- screenshots: paste or attach an image and a vision model reads it —
  screenshot an Amazon Ads dashboard or a KDP bookshelf, say "file this",
  approve the staged campaign or product
- links: give it a product URL and =venture_fetch_url= fills the record in
  from the page, with the address checked against private ranges first
- staged AI writes are approved in the panel: a field-by-field card with
  Apply and Discard, money rendered as money
- AI tool surface: query, get, count, global search and reports, with
  accounts, tokens and chat history walled off from the model entirely
- a dashboard with open-ticket counts, the weighted deal pipeline and an
  activity feed from the audit trail
- invoicing: lines with exact rational money arithmetic, draft → sent →
  paid lifecycle, a printable view, and paid invoices recording their sale
- CSV import for every record type, all-or-nothing with per-row errors
- an Automations page editing the pod rules with live diagnostics from the
  real parser, and hot reload
- a Plugins page with per-plugin YAML configuration that running plugins
  pick up via a change signal, no restart
- users, roles, password changes and session invalidation on sign-out
- a container image and two compose files
- modules: fifteen built in, switched from the configuration, the
  environment or the command line, with dependencies resolved and refused
  at startup; a Modules page, =/api/v1/modules=, =venturectl modules= and
  =venture --list-modules=; plugin modules through
  =venture_context_register_module()=
- record links: any record to any other, with a kind and its inverse, on
  every detail page, over the API, from the CLI and through the assistant,
  validated at the save for every writer
- the software factory: milestones, releases, builds, environments,
  deployments and incidents; CI runs and forge releases ingested through
  the webhook; changelogs drafted from tickets and releases published to the
  forge; the =releases=, =lead_time= and =incidents= reports

Known limits, as opposed to missing pieces:

- The suite runs against SQLite by default. PostgreSQL is exercised by
  bringing up =compose.deploy.yaml=; point =VENTURE_TEST_DB= at an instance
  to run the suite against that dialect too.
- Transactional mail uses the durable SMTP outbox described in
  [[file:docs/mail.org][mail]]. Outreach delivery adapters remain separate
  from its campaign and subscriber records.

** Licence

AGPLv3 or later. See [[file:LICENSE][LICENSE]].
