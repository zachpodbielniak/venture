OIDC demonstration — 2026-09-19

Artifact: DEBUG build/debug/tests/test-oidc, linked to the actual Venture web
server and authentication implementation. Provider: disposable Keycloak 26.7.3,
synthetic realm venture-125-fixture, loopback HTTP with test-only provider-cookie
normalization. The browser flow exercises real Venture HTTP routes, the real
Keycloak HTML login form and a real authorization-code/PKCE exchange. It is an
HTTP browser-protocol demonstration driven by libsoup, not a video or a claim
that production TLS or another identity provider was validated.

Command (SQLite):
VENTURE_TEST_OIDC_KEYCLOAK_ISSUER=http://127.0.0.1:58083/realms/venture-125-fixture build/debug/tests/test-oidc -p /oidc/browser-keycloak -p /oidc/mfa-provenance-recovery -p /oidc/session-expiry-signout

Verbatim behavioral output below; unrelated GLib transport debug lines and
timing lines omitted. No credentials, authorization codes, state values or
session tokens are included. The MFA/revocation cases use the deterministic
RSA-signed fixture provider; browser-keycloak uses the named real provider.

# Local member opened provider linking page: HTTP 200
# Ordinary member cannot administer organization provider credentials: HTTP 403
# Password-confirmed linking callback returned to account settings: HTTP 302
# Subsequent SSO callback issued a session and cleared its browser proof in separate cookies
# Provider session opened the authenticated account page: HTTP 200
# Combined demonstration used real Venture HTTP routes and Keycloak 26.7.3 authorization-code/PKCE login
ok 1 /oidc/browser-keycloak
1..1
# Local administrator recovered with password and an unused MFA recovery code after provider unlinking
ok 2 /oidc/mfa-provenance-recovery
1..2
# Expired provider sessions and sessions predating local sign-out both return HTTP 401
ok 3 /oidc/session-expiry-signout
1..3

The same command passed with VENTURE_TEST_ACCOUNTING_POSTGRES_URI set to the
explicit disposable PostgreSQL fixture (a unique schema, not public). Its
verbatim behavioral output follows:

# Local member opened provider linking page: HTTP 200
# Ordinary member cannot administer organization provider credentials: HTTP 403
# Password-confirmed linking callback returned to account settings: HTTP 302
# Subsequent SSO callback issued a session and cleared its browser proof in separate cookies
# Provider session opened the authenticated account page: HTTP 200
# Combined demonstration used real Venture HTTP routes and Keycloak 26.7.3 authorization-code/PKCE login
ok 1 /oidc/browser-keycloak
1..1
# Local administrator recovered with password and an unused MFA recovery code after provider unlinking
ok 2 /oidc/mfa-provenance-recovery
1..2
# Expired provider sessions and sessions predating local sign-out both return HTTP 401
ok 3 /oidc/session-expiry-signout
1..3
