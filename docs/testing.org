#+title: Testing VENTURE
#+description: Running the suite and what it guarantees
#+authors: Zach Podbielniak
#+categories: projects docs
#+created: [2026-08-14]
#+updated: [2026-09-18]
#+version: 0.6.0

* Testing

#+begin_src sh
make DEBUG=1 test                    # everything
make DEBUG=1 test-one T=test-money   # one binary, verbosely
make DEBUG=1 test-valgrind           # under valgrind, if installed
#+end_src

By default, tests use private SQLite fixtures, so the suite needs no external service. It leaves nothing behind on disk either -- see [[#temporary-directories][Temporary directories]] below, which is a claim the suite now checks rather than one this page merely makes. Set =VENTURE_TEST_DB= to a =postgres://= URI to run the database tests against PostgreSQL instead:

#+begin_src sh
podman run -d --rm --name pg -e POSTGRES_PASSWORD=t -e POSTGRES_USER=venture \
    -e POSTGRES_DB=venture -p 127.0.0.1:55432:5432 postgres:16-alpine
VENTURE_TEST_DB=postgres://venture:t@127.0.0.1:55432/venture \
    ./build/debug/tests/test-database
VENTURE_TEST_DB=postgres://venture:t@127.0.0.1:55432/venture \
    ./build/debug/tests/test-factory-ops
#+end_src

=test-factory-ops= honours the same variable, which puts the factory's reports, orderings and lifecycle rules through PostgreSQL -- the place the two backends disagree about where NULL sorts and what =SUM()= returns.

Each test drops and recreates the schema before it runs. An external database is one shared database for the whole run, and several tests create a venture with the slug "books" -- a UNIQUE column -- so without that reset the second one fails for a reason that has nothing to do with what it tests. Worth knowing what that hid: the PostgreSQL run used to stop at the ninth test, and the money-summation bug behind it went unnoticed because SQLite returns =SUM()= as an integer while PostgreSQL widens it to numeric.

Accounting integration fixtures also support PostgreSQL. They create and drop
a unique schema per fixture and need permission to create schemas; they do not
reset =public=. Use a disposable database for all integration runs:

#+begin_src sh
VENTURE_TEST_ACCOUNTING_POSTGRES_URI=postgres://venture:t@127.0.0.1:55432/venture \
    ./build/debug/tests/test-backup
VENTURE_TEST_ACCOUNTING_POSTGRES_URI=postgres://venture:t@127.0.0.1:55432/venture \
    ./build/debug/tests/test-receivables -p /receivables/deposit-allocations-refund \
    -p /receivables/tax-issue-receipt -p /receivables/foreign-currency-fx
VENTURE_TEST_ACCOUNTING_POSTGRES_URI=postgres://venture:t@127.0.0.1:55432/venture \
    ./build/debug/tests/test-payables -p /payables/credit-void-refund
#+end_src

These cases compare complete accounting snapshots, balances and relationships.
The backup suite also restores between SQLite and PostgreSQL in both directions
and checks rollback after malformed input and an injected late write failure.
Migration integration uses =VENTURE_TEST_MIGRATION_POSTGRES_URI= with
=test-migrations -p /migrations/postgresql= and owns a separate schema.

The whole tree builds with =-Werror= and a wide warning set in =gnu89=. A warning is a latent bug; there are none.

* What the suite pins down

** =test-money=

The cases correspond to ways a naive money implementation loses real money: floating point that cannot hold a dime, rounding that biases upward, a split that drops a cent, a zero-decimal currency inflated a hundredfold, silent overflow, and a tolerant parser that must still reject garbage.

** =test-entity=

The base class is generic, so a bug in it is a bug in every record type at once. These pin down that declarative fields really store values, that JSON round-trips, that an absent member leaves a field alone (which is what gives PATCH semantics), that sensitive fields are omitted rather than nulled, that a diff redacts them, and that the derived money figures — net, deductible, weighted, ROI, idea score — are computed the way the reports expect.

** =test-config=

Most importantly, that the YAML document compiled into the binary actually parses and maps onto every property. Without that test the shipped defaults could silently diverge from the C table and nobody would notice until a setting failed to take effect.

Also that validation refuses a non-loopback bind address with authentication off. That combination is not a configuration choice.

** =test-database=

Round-tripping through both backends, optimistic concurrency detecting a lost update, soft deletion hiding but keeping, the audit trail recording the prompt behind an AI change, the ledger refusing an unbalanced transaction, and the schema gaining a column without a hand-written migration.

That a period counts the fractions of its first second and none of the next period's, and that "after", "up to and including" and BETWEEN put a whole second before its fractions, although stored text sorts the other way.

Two tests exist specifically to prove injection is impossible: a filter naming a field that does not exist is refused, and a hostile *value* is bound as a parameter and simply matches nothing — with the table still there afterwards.

** =test-report=

The arithmetic against known data, not merely that reports run. Also that an unreviewed expense counts as zero in the tax report and that the report says so, that CSV export defuses spreadsheet formula injection, and that HTML export escapes its content.

** =test-bankfeed=

Fake feed import through the bank match seam, idempotent sync of the same
window, service start with no installation credential, injected Teller transport
with no network, and a rolled-back batch on a bad line.


** =test-commerce=

Stateless connector factories and explicit organization vault accounts import
orders through document compose and settlement. Coverage includes account-scoped
replay, two organizations/shops with equal remote IDs, legacy adoption without
rewriting invoices, exact totals and rollback, malformed provider shapes,
rotation/disconnect/module disable during fetch, and main-thread vault checks
for asynchronous plugin transports. Ambient Shopify credentials are ignored;
missing organization configuration refuses import, not startup. PostgreSQL uses
the isolated =VENTURE_TEST_ACCOUNTING_POSTGRES_URI= fixture.

** =test-banking=

Statement import, matching and reconciliation. Rule preview against historical
rows does not post; bulk categorize applies a unique enabled rule and leaves
ambiguous rows unmatched; overlapping FITIDs are refused rather than
duplicated; a transfer posts both ledger sides once; a payment from a
non-1000 bank credits that bank's account.

** =test-receivables=

Receipts, partial and complete allocations, deposits, credit notes,
write-offs and refunds; tax codes with exact rates and a liability report;
foreign invoices paid in book currency with stored exchange rates; external
IDs unique within an organization; immutable financial history; and the
lifecycle signal's veto. Failures injected after a source
write must leave every record and audit entry unchanged. Web, REST, the
real =venturectl= process and its MCP tool produce identical financial
rows, including a staged tool call applied after approval. Each source
operation produces a balanced ledger batch. Historical balances and aging
use dated events, including the exclusive month-end boundary. The module
switch hides its records, reports and settlement actions together.

In a sandbox without a writable dconf runtime directory, run the suite
with =GSETTINGS_BACKEND=memory=. Leave =VENTURE_TEST_DB= unset for SQLite;
that variable selects the external PostgreSQL fixture and its schema reset.

** =test-auth=

The tests where a failure is a security hole. That the three login failure modes — wrong password, unknown user, deactivated account — are textually identical, so the form cannot be used to enumerate accounts. That a token stores only a hash. That password hashes are salted and the iteration count is floored. That comparison is constant time.

** =test-plugin=

The claim under test is that an extension is indistinguishable from a
built-in. The example plugin is loaded for real, and its record type is then
expected to have a table, a registry entry and a round-trip through the
database, while its report is expected to appear in the same registry every
consumer reads. =make test= builds the plugins first so this cannot silently
skip.

Also that one broken plugin or venture type is skipped rather than taking the
server down with it, that a plugin named in =plugins.required= is fatal when
missing, and that loading the same file twice registers it once — two
configured directories can overlap.

The crispy path is exercised for real: a =.c= file is written to a plugin
directory, compiled on demand, loaded, and its report generated. The probe
plugin deliberately uses the reporting engine, which is only visible with
=VENTURE_SERVER_BUILD= defined — a regression in the flags the crispy host
passes would otherwise surface as a baffling failure in somebody's own
plugin instead of here. That is not hypothetical: the flags were wrong until
this test was written.

For automation: the handlers are exercised through
=venture_automation_invoke()=, including that a handler's writes are audited
as the automation rather than as whoever was logged in, and that an audit
entry never raises an event — the check that stops a rule that writes from
triggering itself forever.

** =test-entity=, on generated surfaces

That the declared field order and the declared field kind both survive into
the spec every generated surface reads. Neither can be recovered from the
GObject property: properties come back in their own order, and a long text
field is the same C type as a short string one. Both were lost at some point,
and neither looked like an ordering or declaration problem — one showed up as
a list of empty columns, the other as a Notes box one line tall.

** =test-auth=, on chat conversations

That a stored conversation is readable only by the account that had it: the
other account gets /not found/ — indistinguishable from a thread that never
existed, which matters because thread ids are sequential — and its resume
list does not mention the thread. Also that the generic =/api/v1/chat_*=
surfaces are owner-only, so the scoping cannot be walked around, and that
every chat route refuses anonymous callers.

** =test-database=, on the chat transcript

That messages come back in insertion order with the role enum intact. A
transcript is replayed to the model when a thread resumes; if the role did
not survive, a resumed conversation would replay the assistant's words as
the user's — nothing crashes, the conversation just quietly becomes
nonsense.

** =test-entity=, on the CRM and ticket shape

That a contact points at a company by reference rather than naming one in
text — the free-text version could not answer "everything to do with this
account" and spelled the same company three ways. That one ticket type
carries both internal work and external requests. That a comment's internal
flag survives, since it is the reason a comment is a record at all.

** =test-ticket-relation=

That a ticket can point at any registered type, and that a pair which cannot mean anything is refused before it is written: an unknown type name, an id that is not there, the same pair twice, and a ticket related to itself. Also that the reverse lookup works, since a polymorphic pair is invisible to the machinery that finds records pointing at this one — and that a deleted subject leaves the relation still readable.

** =test-module=

That the built-in module table is in dependency order and every rule of registration holds; that a module whose requirement is off is refused with both named, and that the legacy =*.enabled= switches still count; that a disabled module's types and reports vanish from the registries and its pages, API routes and schema entries answer 404 over a real server, while the sidebar loses the links and the heading; that a written reference into a disabled module is refused with the switch named; and that a plugin's module resolves against the running configuration the moment it is added.

** =test-record-link=

That any two records of any types link, and that one row reads correctly from both ends -- the kind from the source, the inverse from the target. That a link which cannot mean anything is refused: an unknown type, a missing record, a record linked to itself, the same pair twice in either direction, the audit log. That the checks run at the save and not only in the constructor, so a link built by hand -- as the generic API would -- is held to them and gets its labels filled in. That a deleted or switched-off end keeps its label and loses its link.

** =test-factory=

That a =workflow_run= delivery becomes a build when requested and updates the same build when completed, a retried delivery included, and that a bad signature plants nothing. That a release cut on the forge becomes a release here with its version read from the tag, and a deleted one is yanked on the same row. That the changelog is drafted from the tickets by issue type and never overwrites what somebody wrote unless asked. That the lead-time and incident reports measure what they claim. That with the module off every page, type and report is gone and the CI webhook is acknowledged and dropped -- and that flipping the switch on the live configuration is enough to bring it back.

That deliveries arriving late, twice and out of order leave a finished build finished, that a later start takes it back to running with no finish time, that Go's zero time is not a finish time, and that neutral is green. That a release planned here and tagged on the forge by hand is one row and keeps the changelog written here; that an edit to the notes of a release yanked here does not put it back out or move when it shipped; that the late =deleted= for a tag cut again yanks the old row and not the new; that a deletion blanks no URL; and that a release candidate is not shipped. That what needs you, readiness, deploying and rolling back work from the page and the API, with an incident's title escaped on the Factory page, an unusable =organization_id= a 400, and a deployment with no environment refused. That with no assistant configured no assistant is offered and the API says why. That =--replace= and =--prerelease= are options, each refused on the other verb and on any other command, and a stray word after the id is a usage error rather than "no flag".

** =test-factory-ops=

What the factory works out from its records, with no server. That an incident's started and resolved follow its status -- stamped, cleared on reopening, refused when it would end before it began -- and that a deployment, a milestone and a release get their dates the same way, so the environment runs what was last deployed. That the changelog leaves out cancelled tickets. That readiness names each thing in the way -- an open ticket, a ticket linked as blocking, a red build, an open incident naming the release -- passes when they are dealt with, ignores cancelled tickets and an incident in postmortem, and scores a clean release 100. That rolling back marks the current deployment rolled back and restores the release before it, is refused with nothing running or nothing earlier, and that a yanked release deploys nowhere. That the forecast is the pace of the last four weeks, with cancelled tickets neither work left nor work done, =at_risk= when it lands after the due date and =overdue= past it. That a red build gets one ticket, high priority on the default branch, and a green one none. That what needs you is empty on an empty factory, leads with the urgent entry, names the default branch and not a feature branch, and loses an entry when its cause is dealt with. That one run priced in another currency does not blank the run totals. That the delivery report counts a rolled-back deployment as a deployment and a failure, two incidents on one deployment as one failure, a redeploy's tickets once, staging's incidents not at all, and "all time" as having a length; that the releases report counts what is still out; that the incidents report reads the status. And the assistant, against a provider that says what it is told to: that build triage sends the log as data under a system prompt that says not to obey it, is never offered a tool, and holds =category=, =confidence= and =retry= to their vocabulary; that release notes are written from what shipped and not from what was cancelled; that a postmortem reads the incident, the release and the fix ticket and writes nothing; that nothing to read is refused before any model is called; and that the briefing is sent the list the records produce.

** =test-dashboard=

That the dashboards module owns its two types and the widget catalogue has every built-in kind, with the factory ones marked as its. That a plugin can add a kind and a bad or duplicate one is refused. That a slug is derived, normalised and unique. That a widget is refused at the save for a kind, a type, a report or options that do not exist, and accepted for a type or report whose module is merely off. That count, list and breakdown answer from the same rows, with ={me}= substituted for whoever is looking; that a note's markup is escaped and an action with a script href is dropped; that metric, report and chart read one report three ways; that a widget for a module that is off, or of a kind that vanished, is an error in place and never a missing page. That only one dashboard is home, and a personal home is nobody else's. That every template imports, an export re-imports to the same definition, importing twice gives two, and a definition with one bad widget leaves nothing behind. And, over a real server: the whole loop from template to home page to deletion, that another user's personal dashboard answers 404 on the page, the API and the fragment, and that the module switch takes the pages, the API and the sidebar entry with it and leaves =/= as the built-in overview.

** =test-webhook=

Webhooks out, routing, satisfaction and triage. An event is named from the type and the action, and the patterns a webhook subscribes to match exactly, per-type or everything — and =ticket.*= does not swallow =ticket_link=. A change reaches a real HTTP server the test owns, carrying a body whose HMAC the test recomputes, and the delivery is recorded; a webhook subscribed to something else receives nothing, and saving a webhook fires nothing. Ten refusals switch it off and it goes quiet. A rota assigns in turn and does not override a ticket raised for somebody, the narrowest rule wins, least-busy counts, and a rule naming nobody refuses. The support report scores satisfaction over the ratings given. Applying a triage takes what it understands, merges tags rather than replacing them, and ignores an invented priority.

** =test-desk=

The workdesk. The inbox is fed by the audit trail and nobody is told about their own edit: an assignment tells the assignee, a comment tells the mentioned and the watchers, a change tells the watchers what moved, the system's roll-ups tell nobody, and reading is per person. A service-level policy matches most-specific-first, stamps a ticket's clocks once, is stamped by the first visible reply and not by a note, and the sweep marks a breach once. A macro applies in one transaction and an inactive one is refused; worklogs roll up onto the ticket and follow a removal; a sprint counts its points; a bulk edit is all or nothing; the timeline weaves changes, comments and worklogs newest first; a budget warns once and refuses a run once exhausted; an incident opens its fix once, linked; the delivery report reads the four keys.

** =test-forge=

Rule resolution in every direction, including that no rule means nothing runs and that a disabled rule falls through to a broader one. Branch names built from hostile titles are still legal git refnames and cannot be read as command-line flags. The Forgejo client refuses a non-http origin, refuses to follow a redirect (a redirect would carry this install's access token to a host the response chose), escapes repository names so they cannot walk out of the path, and maps status codes onto errors an operator can act on. Webhook signatures: correct, wrong, computed over a different body, absent, and — the one that matters — a forge with no secret configured, which must be refused rather than trusted.

** =test-work-sandbox=

The confinement the in-process runner rests on. Paths inside the workspace resolve; every way out is refused, including =..=, absolute paths, symlinks created during the run, and a sibling directory whose name merely starts with the workspace's. That last one is the classic prefix-match bug and it is the shortest route out of any sandbox. Also asserts the executor offers no shell — by calling =bash= and requiring an error, not by reading the tool list.

** =test-work-service=

That runs are off unless the operator turns them on, that starting one returns while it is still live rather than blocking the main loop, that a ticket with no rule refuses, and that a run left in flight by a previous process is reconciled to =interrupted= rather than resumed.

** =test-customer-health=

That the health band is a function of the thresholds and not a stored label (loosen them and a red company drops to amber); that a touch is any of an interaction, a completed activity or inbound mail through a contact, and that one dated after the reading does not count; that an invoice six days overdue is counted but is not a reason at a 15-day threshold; that the sweep creates one =check in: <company>= for the account owner, never a second while it is planned, another once it is cancelled, and stops at its limit; that the sweep is one transaction, so a refused save part-way through leaves no check-in behind; that the sweep refuses without an organisation, with =activities= off and with the module off; that the churn card gains an =At risk= line with a link only while the module is on; and that the REST route, the CLI verb, the report page filtered to red and the company page all reach the same reading. Every date is anchored on an explicit =as_of= so nothing drifts with the calendar. Written red before the module existed: each case failed on the missing report, the missing module or the missing line.

** =test-automation=

That pod handlers run on the thread that dispatched them. podomation's default handler timeout selects an async dispatch path that runs a synchronous handler on a worker thread while the caller blocks in a nested main loop — and VENTURE's handler writes to the database and reads a cascade guard that is not atomic.

** =test-pnl-cuts=

The four cuts of the P&L, each written to fail before the report existed (the registry lookup returned nothing) and to pass by arithmetic checkable by hand. Revenue by customer: a receipt counts as paid and a refund comes off it, an issue event counts as issued and a credit note comes off it, a draft counts for nothing, a January customer has no February row, the converted lead's source is on the row and =by=source= folds the same figures by it; an unknown =by= is refused, and a euro customer keeps a euro row that the dollar totals note rather than add. Spend by vendor: approved bill lines and expenses sum per vendor with the acquisition share split out, a draft and a March bill count for nothing, =by=category= regroups the same money, an uncategorised expense lands in its own group and a euro bill is noted. Recurring costs: monthly, weekly, daily and yearly schedules scale to the month exactly, a cancelled, a paused and an ended schedule are excluded and counted, an invoice schedule is not a cost, a template with no amount is listed unpriced, ending a running schedule takes it out, and a schedule ending today is still a cost today because =end_at= is the inclusive last date. Cash outlook: eight weekly buckets from the period's start with overdue, later and undated rows, part-paid balances, drafts and paid documents counting for nothing, the running balance from the bank statement, =weeks= narrowing the horizon and refusing 0 or 200, no statement meaning no balance rather than zero, and --- in an install whose calendar is New York --- the self-built start being midnight UTC on the local date, so a bill due on the start day is week 1 and not overdue. The P&L card still shows bank cash and now carries the four links; the =pnl_cuts= switch hides the four reports and leaves =budgets='s =cash_forecast= alone.
** =test-money-calendar=

That the calendar shows what is due, not what was scheduled: month-end and daylight-saving expansion in the schedule's own timezone (a 23:30 Auckland occurrence stays on the 1st where UTC arithmetic would slip to the 2nd), paused, ended, generated and journal schedules left out, bills and invoices at their open balance with settled and draft ones absent, overdue items carried to today with the anchor day kept so the feed UID is stable, dunning steps against the events already recorded, payroll net and liabilities as two events, tax packs by status, and nets per currency that are never added across currencies. The negative cases: an unknown =kind=, a source whose module is off, the whole module off (report gone, events refused, page and feed 404), and the CLI verb without =--from= and =--to=. The test was written before the module existed and did not compile against the baseline.

* Adding tests

The =test-ledger= binary pins the universal posting boundary, including
outer-transaction rollback, veto signals, source-save integration, exchange
valuation, immutable history and authoritative balances. The native plugin
test also posts with the example plugin's real rule. See
[[file:ledger.org][the general journal]] for the contracts under test.

One file per subsystem, =tests/test-<area>.c=, picked up automatically by the Makefile's wildcard. Use the fixture pattern for anything needing a database:

#+begin_src C
g_test_add("/area/behaviour", Fixture, NULL,
           fixture_set_up, test_behaviour, fixture_tear_down);
#+end_src

Write the test around the behaviour that matters rather than the function that implements it, and say in a comment what breaks if it regresses. Several tests in this suite exist because a plausible implementation would have been wrong in a way that only shows up in a tax return.

** Temporary directories
:PROPERTIES:
:CUSTOM_ID: temporary-directories
:END:

A fixture that needs a state directory makes one with =g_dir_make_tmp()= and removes it with =venture_test_remove_tree()= from =tests/venture-test-util.h=. Never =g_rmdir()=, and never =g_file_delete()=: both do nothing at all to a directory that is not empty, and both report it in a way every caller here discarded. So a fixture that wrote a single file inside the directory it made left that directory behind, silently, once per test per run.

Five fixtures did. A full green suite left seventeen directories in =/tmp=, 44 had accumulated over a few hours of ordinary runs, and "All 14 test binaries passed" said nothing about any of it.

=venture_test_remove_tree()= deletes the tree, and checks the canonical path of every entry against the root it was given before touching it -- so a =..= in a name, or a directory reached through a symlink, cannot carry a teardown outside the directory the fixture made. A symlink is unlinked rather than followed, and it is the link's own location that is checked, not where it points.

=make test= gives each run a private =TMPDIR=, which =g_get_tmp_dir()= honours, and =tools/venture-test-litter.sh= compares the directories before the run against the ones after it. A *green* run that left one is a failure:

#+begin_src text
test-litter: the suite passed but left temporary directories behind:
  /tmp/venture-run-YOlWIW/venture-routes-CJ5AV3
test-litter: a fixture removed its file and not the directory holding it
test-litter: use venture_test_remove_tree(), not g_rmdir() or g_file_delete()
#+end_src

It compares the run against itself rather than checking that =/tmp= is empty, because a *failing* run leaves its directories on purpose. A =g_assert= failure aborts the process, so no teardown runs on that path -- which is correct and should stay that way: that directory is the evidence. The private =TMPDIR= is what makes the comparison exact, since two worktrees building at once would otherwise report each other's fixtures.

What it cannot see is a directory whose name is not a =g_dir_make_tmp()= string literal in =tests/test-*.c=; the prefixes are read from those literals rather than written down, so the list grows with the suite instead of drifting from it. =make test='s bare =rmdir= of its own =TMPDIR= is the second net: it succeeds only on an empty directory, so a run that left something under a name the scan cannot match still leaves its =TMPDIR= where somebody will find it.

One more thing it surfaced, which was not litter: =test-work-service= started a coding run and returned without waiting for it, so the service's worker thread was still calling =g_mkdir_with_parents()= for the run's workspace while the teardown was removing the directory above it. =strace= shows the =mkdir= and the =rmdir= interleaved. =settle_runs()= drains the live set first. A test that starts background work and walks away leaves a thread writing into a directory nobody owns any more, whoever wins the race.

* Standalone CLI dependency boundary

The CLI must build without the mail server's GMime development package. Run =make DEBUG=1 -W src/cli/main.c venturectl= without any temporary server dependency prefix to compile and relink the CLI. Server-only pkg-config queries are deferred until a server or test recipe expands its flags; a successful CLI build that still emits missing server-package diagnostics fails this boundary.

The Stripe sub-build consumes the YAML archive produced by the parent build. It must not recompile private YAML objects into that shared archive: a parallel =make DEBUG=1 -j4= may be linking =venturectl= against it. =YAML_OBJECTS== on the Stripe sub-make keeps archive ownership with =dep-yaml-glib= while retaining the declared archive prerequisite.

** =test-accounting-acceptance=

The independent replacement pack: a synthetic business issues a taxed invoice,
takes a partial receipt, records and pays a bill, and keeps subledgers tied to
the GL. A payment that fails after the invoice is issued leaves the journal count unchanged. It does not claim country,
payroll or inventory parity. Invoice detail, cash-basis reports, bank reopen
and the cutover wizard are asserted over HTTP in the module suites.

** =test-tax-filing=

Jurisdiction filing adapters, a fake adapter with no network, the US
sales-tax pack from tax codes and invoice-event tax, missing-country
refusal that names the jurisdiction, draft/reviewed/submitted/acknowledged
with preserved JSON/CSV bytes, amendments that keep the prior pack, and
that turning =tax_filing= off leaves =tax_code= working.

** =test-contractor-tax=

Protected contractor TIN records, 1099-NEC totals from paid vendor bills
at the 600 USD threshold, review/approve before export, and an identical
second export of the same year and vendor.

** =test-setup=

A new legal entity's checklist starts incomplete and the first invoice is
refused until cash, AR, AP, tax, earnings and P&L control accounts are
mapped, in bookkeeping language. Completing setup lets a synthetic business
issue an invoice, take a receipt, record an expense and reconcile a bank
statement. Renumbering the mapped cash account still reports it as cash.

** =test-cutover=

The opening posting rule on a July cutover with March receivables and a closed
March: no income, expense or tax, every journal at the cutoff, an empty tax
liability report and a service-owned =opening_at= stamp. Preview collects
located problems (JSON numbers, numeric strings, non-object rows, duplicate
ids, cross-vendor bill numbers) and the strict money grammar refuses locale
formats unless separators are declared. Trial balance ties and mismatches,
per-currency and per-bank-account totals, prepayments and negative balances,
party matching, and AR dates, paid amounts and totals. An open AR, open AP or
credit row whose party id is missing, empty, unknown to the payload's
=customers[]= / =vendors[]= or names an entry without a name is refused
(=open-ar-customer=, =open-ap-vendor=, =credit-party=) with no company or
document written and the batch left in preview, and the preview row carries
the same rule. Receipts on the cutoff
day are not drift; a payment before the cutoff is, with row evidence kept in
the appended report. Activation re-runs reconcile and closes periods through
the cutoff. Rollback unwinds a migrated credit, a real payment and January
depreciation; preflight lists a foreign credit note and a closed period as
blockers; a batch rolled back frees its numbers and tags for a reimport. A
posting veto after the first documents leaves nothing; the row limit, CSV
templates and CSV payloads, opening stock and its rollback, the module switch,
and the page, preview, action,
template and CSV routes (including the selected legal entity) are covered.

** =test-sales-tax=

Jurisdiction rate windows and address rules from field tables; overlapping
windows for one code refused with the code named, adjacent windows allowed,
bad rate, kind or window refused; issuance picks the jurisdiction from the
customer's address codes and freezes jurisdiction, scaled rate and amount
on the line while an exempt customer or product freezes the jurisdiction
at zero; an explicit tax code overrides the rule; exact code matching and
most-specific-rule precedence; a rule switched off levies nothing while
the line falls back to its own percent; a rate change after issue leaves the line,
the event and the return untouched while a later invoice takes the new
window; =sales_tax_return= per jurisdiction ties to =tax_liability= to the
cent across issued, exempt, coded, percent-only and voided invoices and
credit notes; a migrated invoice stamped =opening-at= stays off the return,
as it does off =tax_liability=, so a cutover's tax is not remitted twice;
the 1099-shaped CSV export; the module off; and the export
route and CLI verb over HTTP. Red before the module: the test does not
compile against the base tree.

** =test-lead-routing=

Routing rules act in position order and the first match wins, an inactive
rule never does; with routing configured and nothing matching the lead stays
unassigned and its timeline says "No rule matched", while an organization
without routing rules keeps the older assignment rules and stays quiet.
Conditions read columns, references by name and custom attributes, and a
rule that does not parse is refused when written. Round-robin rotates fairly
over three team members with a persisted cursor, skips a member whose team or
organization membership was deactivated, and refuses an empty rota. The score
is the sum of matching rules with a history row naming them, an unrelated
edit writes none, a hand-typed score is marked manual and left alone until
cleared or rescored, and history rows and the routing reference cannot be
written generically. Both reports are checked on values, and =venturectl
leads reroute= / =rescore= run against the real binary over HTTP. The binary
did not compile against master before the feature (=VENTURE_LEAD_ROUTING_ASSIGN_USER=
undeclared), which is the red step for a feature made of new types.

** =test-backup-schedule=

A due schedule writes a version-4 snapshot whose size and SHA-256 match the
file and is not dispatched twice on the same day; retention prunes the
oldest successful run and keeps its record; a failed run (an unwritable
destination) records its message and deletes nothing even past retention;
an unparseable cron, an unknown scope and a negative retention are refused
on save; a =backup_run= cannot be written or deleted generically. An
installation schedule copies the whole database, credentials included, and
verifies. Verification ties trial balance, ledger totals and document
counts against a restore into an empty in-memory database, reports a
mismatch once the live books move on, runs inside the sweep when the
schedule says =verify=, and refuses a pruned run or a missing file. The
drill keeps its report as a named run that retention never counts.
Migration 000372 succeeds with the module off and leaves no backup tables;
with it on, both tables exist and hold no invented runs. Over HTTP and the
CLI: the Backups page shows runs and verdicts to any session,
owners trigger run, verify and drill, editors and viewers get 403 from the
page and the REST actions, and the module switch hides everything.
** =test-accountant=

The accountant membership reads and exports every finance-owned type but
payroll, the organisation and company rows and expense-attached documents;
every write and delete is refused naming the role before any row is
touched, and no proposal is raised. Leads, deals, activities, tickets, mail,
sequences, contacts, sales, payroll, teams, memberships, other users,
webhooks and saved views answer not-found for reads and writes alike. A
CPA's memberships in several organisations scope the picker and the books
to exactly those; a token minted as an accountant cannot write after a
promotion. =year_end_pack= yields every file and =index.txt=, names an
omitted member when its module is off, rides the scheduled pack path into
=last_output= and rebuilds its zip from those retained bytes. Over HTTP the
owner invites through the existing user and membership routes; the
accountant lands on =/books= with a Books-only sidebar, switches entity,
downloads the zip, reads and exports the books, and receives 404 for CRM
and 403 naming the role for a write.
** =test-calendar= and =test-calendar-surfaces=

Calendar sync against an in-memory CalDAV fake: a missing secret refused by
name before any connection, the =VENTURE_CALDAV_*= prefix, dated calls and
meetings pushed with the export's UUID-based UIDs, calendar events pulled
into meetings and updated in place by UID, a rerun that fetches and writes
nothing, cancellation on both sides instead of deletion, last-modified
conflict resolution with the loser noted on the timeline, the iCalendar
reader and writer, booking slots in the owner's zone with busy times and
buffers, contact deduplication, double-booking refusal and the organization
sweep. The surfaces binary drives =venturectl calendar sync= and the public
=/book/<slug>= page over HTTP against a private server.
** =test-crm-import=

A HubSpot manifest previews, imports and activates with one =crm_import_row=
per source id, and a rerun creates nothing twice; companies and contacts
dedupe against existing records by normalised domain and email, a
conflicting match is an exception that leaves the record untouched, and
contact-to-company links resolve by source id; deals carry money with
currency, the mapped stage, owner and close date; an unmapped stage, a lost
stage without a loss reason, a missing currency, an =attachments= file and
the "all entities" organization are refused before any write; notes, calls and completed tasks become dated
interactions and open tasks planned activities; rollback on a database with
pre-existing records removes exactly what the import created; the Zoho CRM
and Salesforce built-in column maps and a manifest override are exercised;
generic writes are refused; an unreadable date rolls the whole import back;
a second vendor reusing the first vendor's id creates a new record rather
than matching; migration 000390 records the table pair and succeeds with
the module off; and the REST routes and =venturectl crm= verbs run over HTTP.
** =test-deal-lines=

Deal lines drive the deal value: three units at $19.99 less 1000 basis
points make $53.97, editing and deleting a line recomputes in the same
transaction, a generic write of =value= is refused by name while lines
exist and allowed again once none remain, a second currency is refused and
not persisted, invalid quantities, discounts, prices and references are
refused, and a line cannot move between deals. The quote handoff copies the
lines into =DEAL-<id>=, a rerun revises to =-R2= with the current lines and
no duplicates, refusals (no lines, fractional percent, since-deleted
product after the header is written) leave no quote behind, and the REST
route, the deal page button and =venturectl deal quote= are exercised over
HTTP. Written red first: the binary does not compile without =deal_line=,
and removing the value refusal or the currency check turns the matching
case red.

** =test-sequence-tracking=

Consent is off by default at the organization. With both switches on, a
delivery gets a 64-character token, its links become =/t/c/<token>/<n>=
with the originals retained and the pixel appended, and the token field is
omitted from generic JSON. Either switch off leaves the body untouched. Opens count
once per delivery per UTC day and land on the timeline as outbound
outreach that never exits the enrollment; every click is recorded and
resolves to its URL; unknown tokens and link numbers record nothing;
generic writes and removals of links and events are refused; the per-step
engagement report counts sent, opened, clicked and replies with whole
percent rates; and the pixel, redirect, contact page, report route and
module-off 404s are exercised over real HTTP.
** =test-dedupe=

Name similarity ignores case, punctuation and legal suffixes; each scan
reason (email, phone, website, domain with name, name alone) proposes a pair
with its documented score while unrelated rows do not; a rescan keeps the
same rows and drops a pair that stopped matching; contacts are scanned apart
from companies. A merge re-points contacts, interactions, deals, invoices,
tickets, activities and bills through the field tables, unions the fields
with the survivor winning, notes the loser's values on the timeline,
soft-deletes the loser with =merged_into_id=, closes the candidate and writes
one audit entry; an injected failure after the references moved leaves
nothing changed. Merging across organizations, onto itself, or with issued
documents in a currency the survivor lacks is refused, and the opposite case
accepted; generic writes to candidates and =merged_into_id= are refused; the
module switch hides and restores the scan. REST scan and merge, the page
with a merge button each way, the CLI verbs, the =301= forwarding of a merged
id over =/api/v1= and =/e=, and migration with the module disabled are
covered against a private server.
** =test-support-rollup=

The support rollup's worked example --- two customers, an unfiled ticket
and one from the month before --- checked figure by figure: raised, closed,
open, breaches, median reply and resolve minutes, satisfaction, agent
minutes and the cost per row and in total, with a July ticket's August
minutes kept out of August. The rates and their defaults: =n/a= with no
rate, the worklog =hourly_rate= when =support_hourly_rate= is zero, the
integer rate overriding it, a foreign-currency rate left out and noted, a
flat rate pricing only unlogged tickets, and a third of an hour rounding
once to 20 minutes. Every option and every refusal: =sort= both ways and
on the group column, an unknown column, an unknown option, =min_tickets=
hiding rows without moving the totals and refusing a negative or a word,
=company= narrowing to one customer and refusing text or zero, =group_by=
refusing anything but company or product, and the standard organisation
and venture scope. The product breakdown through a custom field, grouped
and filtered. The one function: =venture_support_rollup_cost()= equals the
report's total and the Support card's number, and a company's share.
Over HTTP: a viewer with a session reads it, an option refusal is a
refusal, the CSV export is the report's own rendering, no session is 401,
and with the =tickets= module off the report is not registered and the
route is 404; the company page shows this month's cost from the same
function and the home page carries the organisation's; and =venturectl
support rollup --from --to --sort= in JSON and CSV, with the missing-span,
wrong-verb and bad-sort refusals.
** =test-docs=

The documentation site (issue #97). A fixture tree renders to the same
shape with the builtin renderer and with Emacs (skipped where Emacs is
absent), and the automatic choice reports which it used. A broken
=file:= link, a link that escapes the checkout, a document the
index does not link (directly or through another document) and a missing
=docs/index.org= each refuse the build and leave no site behind. The real
=docs/= renders with every file a page and the Start here group in the
nav. =venturectl docs build= renders and reports; a broken link exits 8
naming the link, a bad verb or renderer exits 2. A server with
authentication required serves =/docs=, =/docs/index.html=, the
stylesheet, script and search index without a session with their content
types, refuses a directory, an =.org= source and an escape, answers a
never-rendered site with the page that says what to run, and answers 404
throughout with the =docs= module off; =/= still redirects to sign in.
=README.org= and =docs/index.org= point at the site and the quickstart.
Then =tests/docs-quickstart.sh= runs every named block of
=docs/quickstart.org= against a throwaway SQLite instance on a private
port and asserts each step, in order.
** =test-report-delivery=

A report pack's =deliver= and =recipients= rules on save (the default =none=,
an unknown mode, an address without =@=, a double =@=, a trailing comma, a
header-injecting newline, =email= with nobody to mail). A due pack set to
=email= queues one message through the outbox with the recipients, a subject
and body naming the pack and its period, the output JSON and a CSV per
tabular report as retained attachments, and stamps the pack with the
delivery time, Message-ID and outbox row; nothing is sent until the outbox
sweep runs, and a second sweep neither re-runs nor re-mails. A refused
enqueue, the mail module off and no transport configured each record the
reason on the pack, leave the run and its output untouched, do not re-run
the report, and let the sweep reach the next pack. The deliver action
refuses a pack with no retained output and one set to =none=, and re-sends
the retained output as a new message under a new key. The outbox's inline
attachments (refused without a name, without data, with data that is not
base64, and a document reference without attachment storage). The REST
route refuses anonymous (401), viewer (403), staged and unknown-pack
requests and answers an editor with the queued message (202); =report packs
deliver ID= reaches it with an API token, and refuses a missing id.
** =test-mfa=

The second factor end to end: RFC 6238 appendix B vectors, base32, the QR
matrix, enrolment with a pending-secret refresh and the key requirement,
refusal of the password-only sign-in, drift and replay, the five-in-fifteen
failure window, single-use recovery codes and regeneration, audit
attribution and redaction, disable, the owner-only reset through the action
registry and =venturectl=, the =require_mfa_for_admins= setting, and the HTTP
gate, sign-in flow and REST/CSV/audit redaction. Every clock-dependent
check uses =VentureMfaService:fixed-time=; the =000410= guard is proven to
refuse a half-present schema. Needs =venturectl= built, like
=test-auth=.
** =test-plugin= and =test-auth=, on the five-question sidebar

The sidebar is grouped by the question a page answers (=docs/navigation.org=).
=test-plugin= holds the section map to its membership: the five headings in
order, each path a section gathers, that every gathered path is a row in the
link table and is gathered once, and that no row is orphaned by the regrouping.
=test-auth= renders the sidebar and checks the headings are drawn in order
after Overview, a gathered page is drawn once under its question, the emptied
headings are gone, and a module switched off loses its row while the question
keeps its heading. The anonymous-route sweep walks the unchanged link table,
so every URL still redirects when nobody is signed in.
** =test-cash-vs-booked=

The =cash_vs_booked= report over two months of invoices, a void, a credit
note, a second receipt and a refund: booked and received land in the bucket
of their own date, the gap accumulates from the period start, and a
narrower period starts the running total afresh. Week buckets on a period
that opens mid-week are clipped and labelled by their Monday, and the
splitter behind them is checked on its own. Two currencies stay two rows
and the tiles name the currency they left out; =customer_id= keeps one
customer, =venture_id= reaches cash through the venture's invoices and
leaves an unapplied deposit out; a cutover opening invoice is neither
booked nor received; =as_of= stops the evidence. An unknown option and a
=bucket= other than month or week are refused. The Profit and loss home
card's Booked vs received line equals the report's own metric and reads
=n/a= with receivables off. Over the API, the CSV export and =venturectl
report=, =bucket=week= reaches the report, the JSON agrees between the two,
the report page keeps the option in its links, and a bad bucket comes back
422 from both.

* Organization integration credentials

=make DEBUG=1 test-one T=test-integrations= exercises organization/account
binding, authenticated encryption and redaction, rotation, stale versions,
disconnection, historical identity and generic-write/permission refusals.
See [[file:integrations.org][the integration contract]] for what this foundation
does and the provider-level acceptance work still pending.

* Accounting replacement acceptance

=test-accounting-acceptance= now proves opening cash/AR/AP import and replay,
settlement and archive tie-outs; trading through explicit bank matches,
reconciliation, financial statements, signed close, refusal, reopening and
correction; and a successful retry after an injected posting rollback.
All three scenarios pass SQLite and schema-isolated PostgreSQL. See
[[file:accounting-acceptance.org][the exact expected balances and commands]].
This is synthetic executable evidence, not a completed deployment exercise.

* Project profitability and actions

=test-projects= verifies frozen labour cost, unknown historical evidence,
currency and organization integrity, immutable billed evidence, period-based
management financials, source identities, rounding/overflow, and the generic
approve/bill actions with finance access and stale-proposal checks. The nine
cases pass SQLite and schema-isolated PostgreSQL; migration and anonymous-route
coverage run separately. See [[file:projects.org][the profitability contract]].

* Structured calls

=test-activities= has 36 cases, including atomic call history/followup,
lead-only timeline, replay/conflict, staged and stale completion, bounded
inputs and escaped transcripts. All pass on SQLite and the isolated
PostgreSQL fixture; standalone file-upgrade coverage remains SQLite.
=test-desk= passes 16 cases and =test-migrations= passes nine including its
optional PostgreSQL check through migration 000610. See [[file:activities.org][the call contract]].
* Sales to delivery acceptance

=test-project-delivery= has twelve SQLite and PostgreSQL scenarios covering
accepted-quote and won-deal handoff, replay conflicts, ordinary-ticket work,
acceptance, project and quote invoicing, association of an existing invoice,
additive scope, full-billing history, atomic rollback/retry, module boundaries,
revoked membership and stale staged decisions. The related project, progress,
module and migration suites exercise their existing contracts too. See
[[file:project-delivery.org][the operator workflow and financial date semantics]].

* CRM to collection

=test-crm-acceptance= crosses capture/deduplication, ownership-preserving lead
conversion, ordinary activities and transactional mail, quote acceptance,
project delivery and partial invoicing through payment. Its log transport
sends no real mail. The scenario checks source identities and exact minor-unit
balances; see [[file:crm-acceptance.org][the reproducible walkthrough and limits]].

* OCR and attachment ownership

The OCR lane passed fifteen cases on SQLite and isolated PostgreSQL,
including real Tesseract 5.5.3 and a mixed native/scanned PDF. Five independent
attachment tests cover service-owned paths, organization boundaries, legacy
aliases, nested directories, size bounds, symlinks and FIFOs. At that lane
revision capture (6), mail (23), mail sync (38), documents (5), authorization
(92), modules (19), configuration (32) and migrations (7) passed their relevant
lane checks; the migration suite has since grown to nine cases through
migration 000610.
Engine-independent tests still run without optional OCR libraries; real OCR
requires the explicit test executable described in [[file:ocr.org][the OCR guide]].
The combined epic revision is validated separately in the acceptance ledger.
* Organization SMTP routing

=test-mail-routing= verifies main-thread credential selection, account-bound
retry and persistence-before-submission. Its database cases support
=VENTURE_TEST_ACCOUNTING_POSTGRES_URI=. =make DEBUG=1 test-mail-relay= adds
an authenticated STARTTLS Mailpit fixture with two accounts and independent
relay-message assertions. See [[file:mail.org][Transactional mail]] for prerequisites,
cleanup and the distinction between acceptance and inbox delivery.

* Organization bank-feed credentials

=test-bankfeed= now runs seventeen cases on SQLite and isolated PostgreSQL
(=VENTURE_TEST_ACCOUNTING_POSTGRES_URI=). Account-scoped encrypted settings
replace ambient Teller credentials. The suite covers equal provider account
identifiers in two organizations, rotation, disconnect, immutable import
identity, legacy-key migration with the module enabled and disabled, and
revocation while a response is outstanding. A deleted bank is refused before
any provider request; its regression test fails without the deleted-row guard. Removing the completion-time
credential check makes =/bankfeed/inflight-revocation= fail because the
revoked run succeeds; restoring it refuses the import. The settings route
has anonymous/editor refusal and write-only/stale-form checks in =test-auth=.

Provider transport fixtures are synthetic: these checks do not demonstrate
a live Teller enrollment or production mutual TLS. Production/development
configuration requires a valid certificate/private-key pair; sandbox is an
explicit selection. The ordinary DEBUG build and generated GIR also cover
the owned settings-schema and immutable-client extension points.

* Private mailbox and calendar connectors

=test-connectors= covers the shared IMAP/CalDAV credential lifecycle,
private-owner read/audit/vault boundaries, membership revocation, changed
selections and populated additive upgrades. Its database fixtures support
=VENTURE_TEST_ACCOUNTING_POSTGRES_URI=. =test-mail-sync= and =test-calendar=
exercise private import suppression, real stored snapshots and revocation
between transport completion and persistence using the fake clients'
synchronous =response-ready= signal. Restoring a removed private calendar
UID preserves its retained record identity.

The generic routes remain covered by =test-auth=, with assigned-owner
settings, blank saved secrets, stale forms and another member's 404 refusal.
The mail/calendar surface suites verify that CLI sync reports the explicit
operator allowlist refusal instead of resolving old environment labels.
=test-webhook= checks that a shared event selects only its organization's
hooks and a private event selects none. These provider fixtures make no live
mailbox or calendar service claim; the separate browser recording is in
[[file:evidence/125-connectors-demo.org][the connector demonstration]].

* Organization OpenID Connect

=test-oidc= verifies signed-token handling, PKCE/state/nonce, explicit local
linking, organization scope, replay refusal, provider/user/membership revocation,
local-MFA continuation and bounded network failure. Its lane passed 22 SQLite
and isolated PostgreSQL cases and a real Keycloak 26.7.3 browser exercise;
[[file:oidc.org][OIDC documentation]] records the operator procedure and evidence limitations.
The combined epic build also checks anonymous route protection and local login.

* Organization AI providers

=test-ai-organizations= exercises private/platform provider selection, grant
revocation, quota accounting, concurrency, configuration revisions and redaction.
Its PostgreSQL fixtures use =VENTURE_TEST_ACCOUNTING_POSTGRES_URI=. Provider tests
use local controlled HTTP peers; they do not spend against a live model account.
See [[file:ai-organizations.org][the contract and built-artifact demonstration]].

* Recurring invoice automatic collection

=test-stripe= includes controlled-provider authorization, recurring collection,
persisted idempotency and retries, signed settlement and restart scenarios.
Application HTTP responses never constitute payment evidence. See
[[file:stripe-automatic-demonstration.org][the built-artifact collection transcript]] and
[[file:stripe.org][the provider/account contract]]. PostgreSQL fixture runs use
=VENTURE_TEST_ACCOUNTING_POSTGRES_URI=.
* Offline credential maintenance

=make DEBUG=1 test-one T=test-process-lease= checks separate-process exclusion
and crash recovery. =test-integrations= verifies master-key rotation, full rollback,
read-only key checking and private retained history across page boundaries. Both
accept =VENTURE_TEST_ACCOUNTING_POSTGRES_URI= for an isolated PostgreSQL fixture.
See [[file:integration-key-maintenance.org][the maintenance contract]].
* Tenant recovery tooling

=test-tenant-recovery= is part of the ordinary DEBUG suite. It invokes the
real backup tool with a deterministic Podman transport fixture; encryption,
archive parsing, private permissions, maintenance locking and filesystem
publication use real local tools. Install =jq=, =gnupg2=, =tar=, =coreutils=,
=diffutils= (for =cmp=), =gawk=, =findutils=, =util-linux= and =python3= before
running it: the backup tool drives =venture-tenantctl= (a Python program) for
catalog registration and refuses to start without =python3=, and
=test-tenant-lifecycle= is itself a Python harness. Container builds include
these test dependencies. The test refuses wrong ownership/workspace/image,
live processes, unsafe archive entries, bad credentials and nonempty targets,
and verifies successful content restoration and failure cleanup. It does
not substitute for a physical PostgreSQL recovery drill.

* Hosted lifecycle boundaries

=test-tenant-lifecycle= checks the local operator boundary with real filesystem
permissions/locks and controlled container responses. =test-tenant-recovery= also
checks inherited maintenance-descriptor identity. These cases are separate from
physical restore, deployment security and capacity qualification. See
[[file:tenant-operations.org][the lifecycle runbook]].

* Sales and hosted integration regressions

=test-sales-performance= covers organization/team routing, immutable booking
credit and reversals, exact currency-separated quotas, retained evidence while
disabled, and upgrades without invented historical credit.

=test-hosted-maintenance= invokes the built server with bounded subprocesses.
It checks that the process lease precedes offline lifecycle/key commands, hosted
maintenance needs an explicit reason, command combinations are refused, and a
suspended workspace remains suspended after rotation. The old key then fails
verification and the new key succeeds. =test-stripe='s
=/stripe/automatic-tenant-lifecycle= checks background collection refusal in
read-only and suspended states, unchanged scheduling rows, and explicit resumption.
These integration cases supplement the independent service and process-lease tests.

* Hosted integration: CRM rollback and sales credit

The 122-binary integration run exposed =/crm-import/rollback-exact=: the
sales ledger correctly refused deleting a won deal. CRM rollback now uses
the canonical stage transition before removal. The regression asserts the
retained booking plus reversal net to zero, alongside exact retained CRM
rows and repeat rollback. All 13 CRM import cases pass after this repair.
** =test-marketing= and =test-marketing-mail=

Marketing tests retain a frozen deduplicated audience and rendered content,
preserve declared evidence and membership references through CRM merge while
ordinary references move to the survivor, require source/address/purpose
evidence, refuse forged history and reserved
withdrawal identities, and verify same-organization references and editor
roles. Queued delivery rechecks address, suppression and permission immediately
before transport, including a synchronous automation withdrawal during the
last bookkeeping save. Unsubscribe survives a deleted source, unrelated signed-in
principal, and disabled marketing/mail/sequences modules; necessary transactional mail remains
independent and applicable sequence enrollment exits through its existing rule.

File reconnect expires a retained sending lease to =uncertain= without a new
submission or outbox identity. Explicit retry retains that identity. Throttle
coverage distinguishes enqueue time from the later actual delivery clock;
pause defers without spending attempts and cancellation refuses unknown SMTP
outcomes. Public serialization omits private URLs, tokens and payloads.
Tracking checks UTC daily deduplication and stable click destinations; reviewed
temporary feedback does not suppress while hard-bounce evidence does. Approval
cohort reports verify half-open boundaries, organization isolation, and overlapping
SMTP acceptance/hard-bounce counts without inventing delivery or reading proof.

A real loopback HTTP server and the built =venturectl= exercise authenticated
preview/approve/pause/resume plus anonymous one-click unsubscribe and replay.
GET never withdraws permission. =test-auth= pins the new public unknown-token
routes and protected action. =test-mail= inspects both attempts' actual SMTP
wire headers, including the same Message-ID and RFC 8058 header pair.

The paired 420 migration preserves existing CRM/suppression rows without
inventing consent, supports enabling a previously disabled module, and rolls
back a missing identity-index check without losing retained history. Set
=VENTURE_TEST_ACCOUNTING_POSTGRES_URI= to run the complete marketing suite in
fresh per-fixture PostgreSQL schemas; the reconnect case reuses only its own
schema. No fixture resets =public=. The opt-in test database is disposable;
these checks cover logical reconnect and transaction behavior, not physical
recovery performance. See [[file:marketing-demonstration.org][the built-artifact transcripts]].

* First-party attribution and signed Lightsite intake (#121 / #125)

The attribution fixture reuses canonical lead capture/conversion and settlement,
with schema-isolated SQLite/PostgreSQL helpers. It tests separate anonymous and
email permissions, exact source/campaign binding, stable event/submission replay,
withdrawal and expiration, site/visitor quotas, retained conversion evidence,
signed current account configuration, frozen first/last source selection, and
separate won-value/issued-net/applied-cash/refund measures. Financial events use
actual monotonically later receipt instants: no future-dated financial event is
allowed merely to make a fixture convenient.

The Node.js browser harness is invoked by =test-attribution= and is therefore a
required development-test dependency, included in the builder and =list-deps=
packages. It uses only Node built-ins and no network. It proves no storage or
tracking before explicit consent, forms without analytics, no implied email
permission, stable in-memory retries, offline withdrawal stopping new tracking,
and serialized consent responses. The ordinary runtime image does not gain
Node for this feature.

Regression evidence includes generic forged evidence being accepted before the
save guard, immutable site identity changing before its guard, site deletion
breaking withdrawal, capture matching another mailbox incorrectly granting email
permission, and synchronous automation changing that mailbox between matching
and consent. Browser tests also reproduced an older consent response erasing the
newer grant's stored policy. Each was observed red and repaired. Report registration
and explicit conversion-rate expectations were tested red before implementation.
The end-to-end campaign fixture exposed the JSON-field storage contract: metadata
JSON is serialized string storage, so mapping validation/lookup must parse it,
not cast its property pointer to =JsonNode=.

The canonical JSON extraction has a byte-for-byte accounting fingerprint
compatibility test; removing object-key sorting failed the regression, then the
restored implementation passed. Marketing and attribution headers are explicitly
included in =PUBLIC_HDRS=; inspect their function declarations in the generated
GIR as well as requiring a warning-free scanner, rather than mistaking an omitted
public API for a clean annotation check.

Final review regressions also cover a repeated untracked inquiry followed by a
consented observation: first/last source evidence and lookback remain separate.
Private visitor references, replay keys and referrer origins are excluded from
ordinary serialization and audit diffs contain only redacted change markers.
The browser treats a retention-forgotten capability's 404 withdrawal as complete,
while offline failures remain pending and cannot resume tracking. These failures
were reproduced before the respective repairs.

The report transport fixture invokes the built =venturectl= and verifies matching
JSON/CSV source rows plus model/details preservation in page links. Its isolated
loopback server disables authentication solely for option-transport testing;
=test-auth= separately exercises the protected settings routes. The authority
fixture exercises real organization membership for site/configuration changes
and the type-level retention action. The financial journey includes canonical
CRM merge, retained original references and separate EUR/USD buckets. Logical
reconnect preserves exact submission replay and refused withdrawn capabilities.
See [[file:attribution-demonstration.org][the built-artifact transcripts]].

A reentrant =entity-saved= regression reproduces withdrawal during observation.
The standalone withdrawal contract refuses BUSY before any nested mutation;
a follow-up call after the observation commits redacts its linkage and refuses
future tracking. The real public HTTP withdrawal path remains a successful
standalone transaction. This avoids an outer rollback undoing an already
reported privacy decision.

* Close action accessibility (epic #125)

The generated-action regression first failed because =close_task.complete=
was absent from the shared action registry. It now drives opening, all
18 checklist tasks, checks, preparer/reviewer separation and completion
through typed actions. It refuses viewer completion, post-completion task
changes and opening when the close module is disabled. All 11 close tests
pass on SQLite and a disposable PostgreSQL schema; neither result is a
claim of hosted capacity. The authenticated deployment walkthrough also
uses the built server and independent preparer/reviewer sessions.

The added =/close/action-authority= case passes on both SQLite and
PostgreSQL: organization editor membership is refused, promotion to finance
enables opening, an invalid currency is refused, and membership revocation
removes the authority immediately. This extends the close suite to 12 cases.

* Retainer action accessibility (epic #125)

The action-level regression failed because =company.collect_retainer= was
absent. Its repaired path collects USD 250, releases USD 100 then USD 150,
asserts cash USD 250 and zero remaining liability, and refuses another
release, mixed currency and a non-liability account. The six-case SQLite
suite also covers finance membership, immediate revocation, disabled quotes
and generic-write refusal. The original five cases pass in a disposable
PostgreSQL schema; the expanded six-case PostgreSQL run is recorded with
its subsequent verification checkpoint. Actual authenticated server evidence
is separate from these service/action tests.

* Generated invoice calendar dates (epic #125)

The authenticated project walkthrough first refused an ordinary same-day
receipt because a generated progress invoice was issued at a precise
instant. The regression =/progress/calendar-progress= failed on the issue
date comparison before normalization. Normalization alone then failed under
=TZ=Pacific/Kiritimati= with “A financial event cannot be dated in the
future”: today's date encoding can be ahead of the UTC clock. Settlement
now makes an exception for exactly today's calendar date while retaining
strict source chronology and refusing future instants/tomorrow.

All eight progress/retainer tests pass on SQLite and PostgreSQL. Both
calendar cases (progress and full quote handoff) also pass when separately
launched in =Pacific/Kiritimati=. The four authenticated project scenarios
pass without the former precise-receipt diagnostic workaround. Retainer
finance authority, revocation and module-off cases also passed in the
expanded six-case PostgreSQL run before these two calendar cases were added.

The same calendar-date artifact also passes all 61 receivables cases,
19 quote cases and three independent accounting-acceptance cases. These
include chronology, rollback, allocations and restored financial tie-outs;
none required weakening source-event ordering.

* Hosted HTTP startup ordering (epic #125)

The executable regression =/hosted-maintenance/admission-before-database=
sets each hosted dispatch bound to zero and verifies both refusal and the
absence of the database file. It failed before validation moved ahead of
database opening. The combined artifact passes both =test-hosted-maintenance= cases,
the 19 =test-http-limits= transport cases, the one =test-caldav-http= case
and the 15 =test-tenant= cases; the real HTTP/TLS demonstration
also verifies each invalid setting before database creation.
