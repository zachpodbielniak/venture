#+title: VENTURE documentation
#+description: Index of the VENTURE documentation
#+authors: Zach Podbielniak
#+categories: projects docs
#+created: [2026-08-14]
#+updated: [2026-09-12]
#+version: 0.6.0

* VENTURE

An ERP and CRM in one, written in C on GLib/GObject, for running a portfolio of small business ventures rather than one company.

** Documents

- [[file:orgaccess.org][Organization access]] — membership, organization roles and record ownership

- [[file:architecture.org][Architecture]] — how the pieces fit, and why the design is shaped this way
- [[file:quickstart.org][Quickstart]] — build it, run it, record your first sale
- [[file:federation.org][Federation and offline sync]] — opt-in sharing, pinned hosts, separate federation URLs, local working copies and conflict-aware reconnects
- [[file:configuration.org][Configuration]] — every setting, the layering order, and the compiled C configuration
- [[file:migrations.org][Database migrations]] — backend SQL, startup upgrades, history validation and authoring requirements
- [[file:data-model.org][Data model]] — the record types and how to add one
- [[file:api.org][REST API]] — the generated API, filtering, staged writes, and error handling
- [[file:cli.org][venturectl]] — the command line client
- [[file:ai.org][AI]] — tools, the confirmation policy, and the audit trail
- [[file:mcp.org][venturectl mcp]] — serving the API to an outside AI agent over stdio
- [[file:periods.org][Fiscal periods]] — calendars, closing controls and historical reports
- [[file:money.org][Money and accounting]] — exact arithmetic, allocation, deductibility, the ledger
- [[file:ledger.org][General journal]] — atomic posting, immutable journals, rules, reversals and authoritative balances
- [[file:stripe.org][Stripe payments]] — hosted Checkout and verified settlement
- [[file:assets.org][Fixed assets and deferrals]] — book depreciation, monthly releases and period sweeps
- [[file:receivables.org][Customer receivables]] — receipts, credits, refunds and historical customer balances
- [[file:payables.org][Vendor payables]] — supplier bills, payments and dated liability evidence
- [[file:reporting.org][Reporting]] — the built-in reports and how to write one
- [[file:forge.org][Git forges]] — linking tickets to repositories, and letting an agent do the work
- [[file:knowledge-bases.org][Knowledge bases]] — documents the assistant can read, and how they reach it
- [[file:automation.org][Automation]] — scheduled rules, and the podomation engine
- [[file:modules.org][Modules]] — switching pieces of VENTURE on and off, and what a module is made of
- [[file:parity.org][Accounting parity]] — the gap branch for issue #70, and how work lands on it
- [[file:factory.org][The software factory]] — milestones, releases, builds, environments, deployments, incidents
- [[file:dashboards.org][Dashboards]] — pages of widgets, as many as you like, from any module's records and reports
- [[file:webhooks.org][Webhooks out]] — telling something outside that a record changed: events, signing, deliveries
- [[file:workdesk.org][The workdesk]] — the inbox and watching, saved views, service levels, macros, worklogs, sprints, bulk edits, the timeline, mission control and agent budgets
- [[file:harness.org][The agent harness]] — a coding agent kept open in a workspace: sessions, providers, and what an operator has to switch on
- [[file:interface.org][The interface]] — the design system behind the web UI: substrates, the two registers of type, geometry, and the tokens a plugin builds on
- [[file:extending.org][Extending]] — plugins, crispy scripts, declarative venture types, plugin modules
- [[file:containers.org][Containers]] — the Containerfile, local compose, and a deployment example
- [[file:reconciliation.org][Reconciliation suggestions]] — pluggable matching and operator approval
- [[file:mail.org][Transactional mail]] — durable outbox, SMTP, templates and operator-resolved uncertainty
- [[file:sequences.org][Follow-up sequences]] — enrollment, scheduling, exits, delivery evidence and the mail adapter seam
- [[file:testing.org][Testing]] — running the suite, and what it guarantees
- [[file:leads.org][Leads]] — public capture, duplicate handling, assignment, qualification, conversion and recycling
- [[file:activities.org][Planned activities]] — tasks, calls, meetings, reminders and the daily rep worklist
- [[file:actions.org][Record actions]] — shared business operations and approval

- [[file:banking.org][Banking]] — statement import, matching and reconciliation
- [[file:cutover.org][Accounting cutover]] — guided Zoho Books and QuickBooks opening balances
- [[file:statements.org][Financial statements]] — posted ledger balances, statements, comparatives and P&L reconciliation

- [[file:quotes.org][Commercial quotes]] — price lists, revisions, public acceptance and invoice handoff

** The shape of the thing in one page

Everything derives from GObject properties. A record type is a field table; from that VENTURE builds the database table, the REST resource, the JSON and YAML serialisation, the web form, the AI tool schema, the audit diff and the CLI subcommands. Adding a field is a line; adding a record type is a registration.

Money is never a double. =VentureMoney= is an exact integer count of minor units plus a currency and its exponent. Every operation checks for overflow, rounding is half to even, splitting preserves the total exactly, and arithmetic across currencies is refused rather than guessed.

Nothing is deleted. Deletion stamps a time; the row stays, so a report over a past period still reconstructs and an accidental deletion is recoverable.

Everything is a module, and every module is a switch. The CRM, invoicing, the knowledge bases, the software factory: each is a named group of record types, reports, pages and services that a configuration turns off as one thing, and a module whose requirement is off is refused at startup rather than run half-working.

Any record links to any other. A =record_link= names both ends by type and id and carries a kind with an inverse, so a release points at the tickets it shipped and an incident at the deployment that caused it, on every record's page, through the API, the CLI and the AI.

Something outside can be told too. A webhook is a URL, a set of events and a signing secret; every audited change matching it is POSTed with an HMAC-SHA256 over the body, asynchronously so no write waits on somebody else's server, and every attempt is kept. Ten refusals in a row switch a dead endpoint off and say so.

You are told what matters, and only that. Every record's page has a Watch button and a timeline; a comment naming =@you=, a ticket handed to you, a change to something you follow, a service level about to be missed, a budget crossed or a run finishing lands in your inbox, fed by the audit trail so every door tells the same people the same things. Saved views keep a filtered list under a name; a service-level policy puts clocks on a ticket; a macro answers in one press; sprints burn down; forty records change in one transaction; and the coding runs have one screen, a cost, and a budget that refuses them before the money is gone.

Any page you want, you build. A dashboard is a grid of widgets — a count, a list, a report's figure, a breakdown, a note, a row of buttons — each a declarative question answered from any module's records or reports, rendered the same for the browser, the API and the assistant. There can be as many as you like; one can be home; a personal one is yours alone; and a dashboard exports as JSON that imports anywhere.

The AI reads freely and cannot write unsupervised. A tool call that would change a record stages a diff for approval instead; only approval applies it, and the applied write is audited with the prompt that caused it. Any REST write can do the same with =?stage=1=, so an outside agent proposes into the same queue and a person answers both from one page.

Two binaries, both statically linked against every vendored dependency: =venture= is the server, =venturectl= is the client. Either builds alone.

- [[file:billing.org][SaaS billing]] — plans, customer subscriptions and recurring revenue
- [[file:pipelines.org][Sales pipelines]] — configurable stages, next actions, history, loss reasons and forecasts
- [[file:autojournal.org][Automatic journals]] — configurable sale/expense legs, dated refunds and backfill
