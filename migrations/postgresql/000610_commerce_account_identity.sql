-- Account identity links are created from registered metadata.
-- Legacy invoices and companies remain unbound until explicit audited adoption;
-- no account is inferred from ambient credentials, email or default organization.
SELECT 1;
